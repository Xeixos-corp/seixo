// This file was autogenerated by some hot garbage in the `uniffi-bindgen-react-native` crate.
// Trust me, you don't want to mess with it!
/* tslint:disable */
/* eslint-disable */
// @ts-nocheck
import nativeModule from "./signal_native-ffi";
import { type UniffiRustFutureContinuationCallback, type UniffiForeignFutureDroppedCallback, type UniffiForeignFutureDroppedCallbackStruct,
} from "./signal_native-ffi";
import { type FfiConverter, type UniffiByteArray, type UniffiGcObject, type UniffiHandle, type UniffiObjectFactory, AbstractFfiConverterByteArray, FfiConverterInt32, FfiConverterObject, FfiConverterUInt32, FfiConverterUInt8, RustBuffer, UniffiAbstractObject, UniffiError, UniffiInternalError, UniffiRustCaller, destructorGuardSymbol, pointerLiteralSymbol, uniffiCreateFfiConverterString, uniffiCreateRecord, uniffiTypeNameSymbol, variantOrdinalSymbol,
} from "@ubjs/core";
const uniffiCaller = new UniffiRustCaller(() => ({ code: 0 }));

const uniffiIsDebug =
  // @ts-ignore -- The process global might not be defined
  typeof process !== "object" ||
  // @ts-ignore -- The process global might not be defined
  process?.env?.NODE_ENV !== "production" ||
  false;

// Public interface members begin here.

// Hermes (React Native ≥ 0.74) ships TextEncoder and encodeInto, but not
// TextDecoder. For single-string decode (bytesToString), we polyfill via the
// C++ string_from_buffer helper using a duck-typed object matching the
// standard TextDecoder.decode signature. Once Hermes ships a real
// TextDecoder, the `typeof` check will pick it up automatically.
//
// For array-of-strings decode (readStringFromBuffer), we keep a dedicated C++
// helper: the polyfill path (new Uint8Array view + decode) measured ~40%
// slower on getStringArray benchmarks than a direct (buf, offset, length)
// call, due to the per-read view allocation and extra property lookups in
// string_from_buffer.
const stringConverter = (() => {
    const encoder = new TextEncoder();
    const decoder: { decode(input: UniffiByteArray): string } =
        typeof TextDecoder !== "undefined"
            ? new TextDecoder()
            : {
                  decode: (bytes: UniffiByteArray) =>
                      nativeModule().ubrn_uniffi_internal_fn_func_ffi__string_from_buffer(
                          bytes,
                          undefined as any,
                      ) as string,
              };
    return {
        // Single-string lower() uses the C++ helper — TextEncoder.encode
        // measured ~43% slower on takeString benchmarks.
        stringToBytes: (s: string) =>
            nativeModule().ubrn_uniffi_internal_fn_func_ffi__string_to_buffer(s, undefined as any),
        bytesToString: (ab: UniffiByteArray) => decoder.decode(ab),
        // Direct C++ call — bypasses uniffiCaller.rustCall() overhead.
        // Matters for N-element arrays.
        stringByteLength: (s: string) =>
            nativeModule().ubrn_uniffi_internal_fn_func_ffi__string_to_byte_length(s, undefined as any) as number,
        // Encode directly into the RustBuffer backing store via
        // TextEncoder.encodeInto — zero intermediate allocation. Replaces
        // the old C++ write_string_into_buffer helper.
        writeStringIntoBuffer: (s: string, buf: any, offset: number): number => {
            const view = new Uint8Array(
                buf.arrayBuffer,
                offset,
                buf.arrayBuffer.byteLength - offset,
            );
            return encoder.encodeInto(s, view).written;
        },
        // Dedicated C++ helper — avoids per-read Uint8Array allocation and
        // the double property-lookup in string_from_buffer.
        readStringFromBuffer: (buf: any, offset: number, length: number): string =>
            nativeModule().ubrn_uniffi_internal_fn_func_ffi__read_string_from_buffer(buf, offset, length) as string,
    };
})();
const FfiConverterString = uniffiCreateFfiConverterString(stringConverter);

export type EncryptedEnvelope = {
    /**
     * libsignal_protocol::CiphertextMessageType as a raw byte (2 = Whisper/
     * ordinary ratchet message, 3 = PreKey/first message in a new session).
     */
    messageType: number,
    ciphertextBase64: string
}

/**
 * Generated factory for {@link EncryptedEnvelope} record objects.
 */
export const EncryptedEnvelope = (() => {
    const defaults = () => ({
    });
    const create = (() => {
        return uniffiCreateRecord<EncryptedEnvelope, ReturnType<typeof defaults>>(defaults);
    })();
    return Object.freeze({
        create,
        new: create,
        defaults: () => Object.freeze(defaults()) as Partial<EncryptedEnvelope>,
    });
})();

const FfiConverterTypeEncryptedEnvelope = (() => {
    type TypeName = EncryptedEnvelope;
    class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
        read(from: RustBuffer): TypeName {
            return {
                messageType: FfiConverterUInt8.read(from), 
                ciphertextBase64: FfiConverterString.read(from)
            };
        }
        write(value: TypeName, into: RustBuffer): void {
            FfiConverterUInt8.write(value.messageType, into);
            FfiConverterString.write(value.ciphertextBase64, into);
        }
        allocationSize(value: TypeName): number {
            return FfiConverterUInt8.allocationSize(value.messageType) +
             FfiConverterString.allocationSize(value.ciphertextBase64);
            
        }
    };
    return new FFIConverter();
})();

/**
 * The public half of a device's prekey material, as published to the
 * server (Supabase `signed_prekeys` / `one_time_prekeys` tables) for other
 * devices to fetch and use in `SignalDevice::establish_session`.
 */
export type PreKeyBundleData = {
    registrationId: number,
    deviceId: number,
    identityKeyBase64: string,
    oneTimePrekeyId: number,
    oneTimePrekeyPublicBase64: string,
    signedPrekeyId: number,
    signedPrekeyPublicBase64: string,
    signedPrekeySignatureBase64: string,
    kyberPrekeyId: number,
    kyberPrekeyPublicBase64: string,
    kyberPrekeySignatureBase64: string
}

/**
 * Generated factory for {@link PreKeyBundleData} record objects.
 */
export const PreKeyBundleData = (() => {
    const defaults = () => ({
    });
    const create = (() => {
        return uniffiCreateRecord<PreKeyBundleData, ReturnType<typeof defaults>>(defaults);
    })();
    return Object.freeze({
        create,
        new: create,
        defaults: () => Object.freeze(defaults()) as Partial<PreKeyBundleData>,
    });
})();

const FfiConverterTypePreKeyBundleData = (() => {
    type TypeName = PreKeyBundleData;
    class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
        read(from: RustBuffer): TypeName {
            return {
                registrationId: FfiConverterUInt32.read(from), 
                deviceId: FfiConverterUInt32.read(from), 
                identityKeyBase64: FfiConverterString.read(from), 
                oneTimePrekeyId: FfiConverterUInt32.read(from), 
                oneTimePrekeyPublicBase64: FfiConverterString.read(from), 
                signedPrekeyId: FfiConverterUInt32.read(from), 
                signedPrekeyPublicBase64: FfiConverterString.read(from), 
                signedPrekeySignatureBase64: FfiConverterString.read(from), 
                kyberPrekeyId: FfiConverterUInt32.read(from), 
                kyberPrekeyPublicBase64: FfiConverterString.read(from), 
                kyberPrekeySignatureBase64: FfiConverterString.read(from)
            };
        }
        write(value: TypeName, into: RustBuffer): void {
            FfiConverterUInt32.write(value.registrationId, into);
            FfiConverterUInt32.write(value.deviceId, into);
            FfiConverterString.write(value.identityKeyBase64, into);
            FfiConverterUInt32.write(value.oneTimePrekeyId, into);
            FfiConverterString.write(value.oneTimePrekeyPublicBase64, into);
            FfiConverterUInt32.write(value.signedPrekeyId, into);
            FfiConverterString.write(value.signedPrekeyPublicBase64, into);
            FfiConverterString.write(value.signedPrekeySignatureBase64, into);
            FfiConverterUInt32.write(value.kyberPrekeyId, into);
            FfiConverterString.write(value.kyberPrekeyPublicBase64, into);
            FfiConverterString.write(value.kyberPrekeySignatureBase64, into);
        }
        allocationSize(value: TypeName): number {
            return FfiConverterUInt32.allocationSize(value.registrationId) +
             FfiConverterUInt32.allocationSize(value.deviceId) +
             FfiConverterString.allocationSize(value.identityKeyBase64) +
             FfiConverterUInt32.allocationSize(value.oneTimePrekeyId) +
             FfiConverterString.allocationSize(value.oneTimePrekeyPublicBase64) +
             FfiConverterUInt32.allocationSize(value.signedPrekeyId) +
             FfiConverterString.allocationSize(value.signedPrekeyPublicBase64) +
             FfiConverterString.allocationSize(value.signedPrekeySignatureBase64) +
             FfiConverterUInt32.allocationSize(value.kyberPrekeyId) +
             FfiConverterString.allocationSize(value.kyberPrekeyPublicBase64) +
             FfiConverterString.allocationSize(value.kyberPrekeySignatureBase64);
            
        }
    };
    return new FFIConverter();
})();


// Flat error type: SignalNativeError
export enum SignalNativeError_Tags {
    Protocol = "Protocol",
    Base64 = "Base64",
    Utf8 = "Utf8",
    InvalidDeviceId = "InvalidDeviceId"
}
export const SignalNativeError = (() => {
    class Protocol extends UniffiError {
        /**
         * @private
         * This field is private and should not be used.
         */
        readonly [uniffiTypeNameSymbol]: string = "SignalNativeError";
        /**
         * @private
         * This field is private and should not be used.
         */
        readonly [variantOrdinalSymbol] = 1;

        readonly tag = SignalNativeError_Tags.Protocol;

        constructor(message: string) {
            super("SignalNativeError", "Protocol", message);
        }

        static instanceOf(e: any): e is Protocol {
            return (
                instanceOf(e) && (e as any)[variantOrdinalSymbol] === 1
            );
        }
    }
    class Base64 extends UniffiError {
        /**
         * @private
         * This field is private and should not be used.
         */
        readonly [uniffiTypeNameSymbol]: string = "SignalNativeError";
        /**
         * @private
         * This field is private and should not be used.
         */
        readonly [variantOrdinalSymbol] = 2;

        readonly tag = SignalNativeError_Tags.Base64;

        constructor(message: string) {
            super("SignalNativeError", "Base64", message);
        }

        static instanceOf(e: any): e is Base64 {
            return (
                instanceOf(e) && (e as any)[variantOrdinalSymbol] === 2
            );
        }
    }
    class Utf8 extends UniffiError {
        /**
         * @private
         * This field is private and should not be used.
         */
        readonly [uniffiTypeNameSymbol]: string = "SignalNativeError";
        /**
         * @private
         * This field is private and should not be used.
         */
        readonly [variantOrdinalSymbol] = 3;

        readonly tag = SignalNativeError_Tags.Utf8;

        constructor(message: string) {
            super("SignalNativeError", "Utf8", message);
        }

        static instanceOf(e: any): e is Utf8 {
            return (
                instanceOf(e) && (e as any)[variantOrdinalSymbol] === 3
            );
        }
    }
    class InvalidDeviceId extends UniffiError {
        /**
         * @private
         * This field is private and should not be used.
         */
        readonly [uniffiTypeNameSymbol]: string = "SignalNativeError";
        /**
         * @private
         * This field is private and should not be used.
         */
        readonly [variantOrdinalSymbol] = 4;

        readonly tag = SignalNativeError_Tags.InvalidDeviceId;

        constructor(message: string) {
            super("SignalNativeError", "InvalidDeviceId", message);
        }

        static instanceOf(e: any): e is InvalidDeviceId {
            return (
                instanceOf(e) && (e as any)[variantOrdinalSymbol] === 4
            );
        }
    }

    // Utility function which does not rely on instanceof.
    function instanceOf(e: any): e is SignalNativeError {
        return (e as any)[uniffiTypeNameSymbol] === "SignalNativeError";
    }
    return {
        Protocol,
        Base64,
        Utf8,
        InvalidDeviceId,
        instanceOf,
    };
})();

// Union type for SignalNativeError error type.
export type SignalNativeError = InstanceType<
    typeof SignalNativeError['Protocol' | 'Base64' | 'Utf8' | 'InvalidDeviceId']
>;

const FfiConverterTypeSignalNativeError = (() => {
    const intConverter = FfiConverterInt32;
    type TypeName = SignalNativeError;
    class FfiConverter extends AbstractFfiConverterByteArray<TypeName> {
        read(from: RustBuffer): TypeName {
            switch (intConverter.read(from)) {
                case 1: return new SignalNativeError.Protocol(
                    FfiConverterString.read(from)
                );
            
                case 2: return new SignalNativeError.Base64(
                    FfiConverterString.read(from)
                );
            
                case 3: return new SignalNativeError.Utf8(
                    FfiConverterString.read(from)
                );
            
                case 4: return new SignalNativeError.InvalidDeviceId(
                    FfiConverterString.read(from)
                );
            
                default: throw new UniffiInternalError.UnexpectedEnumCase();
            }
        }
        write(value: TypeName, into: RustBuffer): void {
            const obj = value as any;
            const index = obj[variantOrdinalSymbol] as number;
            intConverter.write(index, into);
        }
        allocationSize(value: TypeName): number {
            return intConverter.allocationSize(0);
        }
    }
    return new FfiConverter();
})();

/**
 * One device's worth of Signal Protocol state: its identity keypair plus
 * the session/prekey stores. Backed by libsignal's `InMemSignalProtocolStore`
 * (in-memory only) — persisting this to Keychain/Keystore-backed storage is
 * a follow-up milestone, not yet done here.
 */
export interface SignalDeviceLike {
    
/**
 * Decrypts a message received from `remote_user_id`/`remote_device_id`,
 * transparently completing session establishment if this is the first
 * (PreKey-type) message of a new incoming session.
 */
    decrypt(remoteUserId: string, remoteDeviceId: number, envelope: EncryptedEnvelope) /*throws*/: string;
/**
 * Encrypts `plaintext` for `remote_user_id`/`remote_device_id` using the
 * existing (or just-established) session, advancing the Double Ratchet.
 */
    encrypt(remoteUserId: string, remoteDeviceId: number, plaintext: string) /*throws*/: EncryptedEnvelope;
/**
 * X3DH/PQXDH: consume a peer's published prekey bundle and derive the
 * initial Double Ratchet session state for talking to them.
 */
    establishSession(remoteUserId: string, remoteDeviceId: number, bundle: PreKeyBundleData) /*throws*/: void;
/**
 * Generates one fresh one-time prekey, one signed prekey, and one Kyber
 * (PQXDH) prekey; stores the private halves locally (needed later to
 * decrypt the first incoming message of a new session) and returns the
 * public bundle to publish to the server.
 */
    generatePrekeyBundle(oneTimePrekeyId: number, signedPrekeyId: number, kyberPrekeyId: number) /*throws*/: PreKeyBundleData;
    identityPublicKeyBase64(): string;
}
/**
 * @deprecated Use `SignalDeviceLike` instead.
 */
export type SignalDeviceInterface = SignalDeviceLike;


/**
 * One device's worth of Signal Protocol state: its identity keypair plus
 * the session/prekey stores. Backed by libsignal's `InMemSignalProtocolStore`
 * (in-memory only) — persisting this to Keychain/Keystore-backed storage is
 * a follow-up milestone, not yet done here.
 */
export class SignalDevice extends UniffiAbstractObject implements SignalDeviceLike {

    readonly [uniffiTypeNameSymbol] = "SignalDevice";
    readonly [destructorGuardSymbol]: UniffiGcObject;
    readonly [pointerLiteralSymbol]: UniffiHandle;
    constructor(userId: string, deviceId: number) /*throws*/ {
        super();
        const pointer =
            uniffiCaller.rustCallWithError(
            /*liftError:*/ FfiConverterTypeSignalNativeError.lift.bind(FfiConverterTypeSignalNativeError),
            /*caller:*/ (callStatus) => {
                return nativeModule().ubrn_uniffi_signal_native_fn_constructor_signaldevice_new(
        FfiConverterString.lower(userId, nativeModule().rustbuffer_alloc),
        FfiConverterUInt32.lower(deviceId, nativeModule().rustbuffer_alloc),
                callStatus);
            },
            /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    );
        this[pointerLiteralSymbol] = pointer;
        this[destructorGuardSymbol] = uniffiTypeSignalDeviceObjectFactory.bless(pointer);
    }

    

    
/**
 * Decrypts a message received from `remote_user_id`/`remote_device_id`,
 * transparently completing session establishment if this is the first
 * (PreKey-type) message of a new incoming session.
 */
    decrypt(remoteUserId: string, remoteDeviceId: number, envelope: EncryptedEnvelope): string /*throws*/ {
    return ((__rb: Uint8Array) => {
        try {
            return FfiConverterString.lift(__rb);
        } finally {
            nativeModule().rustbuffer_free(__rb);
        }
    })(uniffiCaller.rustCallWithError(
            /*liftError:*/ FfiConverterTypeSignalNativeError.lift.bind(FfiConverterTypeSignalNativeError),
            /*caller:*/ (callStatus) => {
                return nativeModule().ubrn_uniffi_signal_native_fn_method_signaldevice_decrypt(
                uniffiTypeSignalDeviceObjectFactory.clonePointer(this),
        FfiConverterString.lower(remoteUserId, nativeModule().rustbuffer_alloc),
        FfiConverterUInt32.lower(remoteDeviceId, nativeModule().rustbuffer_alloc),
        FfiConverterTypeEncryptedEnvelope.lower(envelope, nativeModule().rustbuffer_alloc),
                callStatus);
            },
            /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    ));
    }
    
/**
 * Encrypts `plaintext` for `remote_user_id`/`remote_device_id` using the
 * existing (or just-established) session, advancing the Double Ratchet.
 */
    encrypt(remoteUserId: string, remoteDeviceId: number, plaintext: string): EncryptedEnvelope /*throws*/ {
    return ((__rb: Uint8Array) => {
        try {
            return FfiConverterTypeEncryptedEnvelope.lift(__rb);
        } finally {
            nativeModule().rustbuffer_free(__rb);
        }
    })(uniffiCaller.rustCallWithError(
            /*liftError:*/ FfiConverterTypeSignalNativeError.lift.bind(FfiConverterTypeSignalNativeError),
            /*caller:*/ (callStatus) => {
                return nativeModule().ubrn_uniffi_signal_native_fn_method_signaldevice_encrypt(
                uniffiTypeSignalDeviceObjectFactory.clonePointer(this),
        FfiConverterString.lower(remoteUserId, nativeModule().rustbuffer_alloc),
        FfiConverterUInt32.lower(remoteDeviceId, nativeModule().rustbuffer_alloc),
        FfiConverterString.lower(plaintext, nativeModule().rustbuffer_alloc),
                callStatus);
            },
            /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    ));
    }
    
/**
 * X3DH/PQXDH: consume a peer's published prekey bundle and derive the
 * initial Double Ratchet session state for talking to them.
 */
    establishSession(remoteUserId: string, remoteDeviceId: number, bundle: PreKeyBundleData): void /*throws*/ {uniffiCaller.rustCallWithError(
            /*liftError:*/ FfiConverterTypeSignalNativeError.lift.bind(FfiConverterTypeSignalNativeError),
            /*caller:*/ (callStatus) => { nativeModule().ubrn_uniffi_signal_native_fn_method_signaldevice_establish_session(
                uniffiTypeSignalDeviceObjectFactory.clonePointer(this),
        FfiConverterString.lower(remoteUserId, nativeModule().rustbuffer_alloc),
        FfiConverterUInt32.lower(remoteDeviceId, nativeModule().rustbuffer_alloc),
        FfiConverterTypePreKeyBundleData.lower(bundle, nativeModule().rustbuffer_alloc),
                callStatus);
            },
            /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    );
    }
    
/**
 * Generates one fresh one-time prekey, one signed prekey, and one Kyber
 * (PQXDH) prekey; stores the private halves locally (needed later to
 * decrypt the first incoming message of a new session) and returns the
 * public bundle to publish to the server.
 */
    generatePrekeyBundle(oneTimePrekeyId: number, signedPrekeyId: number, kyberPrekeyId: number): PreKeyBundleData /*throws*/ {
    return ((__rb: Uint8Array) => {
        try {
            return FfiConverterTypePreKeyBundleData.lift(__rb);
        } finally {
            nativeModule().rustbuffer_free(__rb);
        }
    })(uniffiCaller.rustCallWithError(
            /*liftError:*/ FfiConverterTypeSignalNativeError.lift.bind(FfiConverterTypeSignalNativeError),
            /*caller:*/ (callStatus) => {
                return nativeModule().ubrn_uniffi_signal_native_fn_method_signaldevice_generate_prekey_bundle(
                uniffiTypeSignalDeviceObjectFactory.clonePointer(this),
        FfiConverterUInt32.lower(oneTimePrekeyId, nativeModule().rustbuffer_alloc),
        FfiConverterUInt32.lower(signedPrekeyId, nativeModule().rustbuffer_alloc),
        FfiConverterUInt32.lower(kyberPrekeyId, nativeModule().rustbuffer_alloc),
                callStatus);
            },
            /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    ));
    }
    
    identityPublicKeyBase64(): string {
    return ((__rb: Uint8Array) => {
        try {
            return FfiConverterString.lift(__rb);
        } finally {
            nativeModule().rustbuffer_free(__rb);
        }
    })(uniffiCaller.rustCall(
            /*caller:*/ (callStatus) => {
                return nativeModule().ubrn_uniffi_signal_native_fn_method_signaldevice_identity_public_key_base64(
                uniffiTypeSignalDeviceObjectFactory.clonePointer(this),
                callStatus);
            },
            /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    ));
    }
    

    uniffiDestroy(): void {
        const ptr = (this as any)[destructorGuardSymbol];
        if (ptr !== undefined) {
            const pointer = uniffiTypeSignalDeviceObjectFactory.pointer(this);
            uniffiTypeSignalDeviceObjectFactory.freePointer(pointer);
            uniffiTypeSignalDeviceObjectFactory.unbless(ptr);
            delete (this as any)[destructorGuardSymbol];
        }
    }

    static instanceOf(obj_: any): obj_ is SignalDevice {
        return uniffiTypeSignalDeviceObjectFactory.isConcreteType(obj_);
    }

    
}

const uniffiTypeSignalDeviceObjectFactory: UniffiObjectFactory<SignalDeviceLike> = (() => {
    
    return {
    create(pointer: UniffiHandle): SignalDeviceLike {
        const instance = Object.create(SignalDevice.prototype);
        instance[pointerLiteralSymbol] = pointer;
        instance[destructorGuardSymbol] = this.bless(pointer);
        instance[uniffiTypeNameSymbol] = "SignalDevice";
        return instance;
    },

    
    bless(p: UniffiHandle): UniffiGcObject {
        return uniffiCaller.rustCall(
            /*caller:*/ (status) =>
                nativeModule().ubrn_uniffi_internal_fn_method_signaldevice_ffi__bless_pointer(p, status),
            /*liftString:*/ FfiConverterString.lift
        );
    },

    unbless(ptr_: UniffiGcObject) {
        ptr_.markDestroyed();
    },

    pointer(obj_: SignalDeviceLike): UniffiHandle {
        if ((obj_ as any)[destructorGuardSymbol] === undefined) {
            throw new UniffiInternalError.UnexpectedNullPointer();
        }
        return (obj_ as any)[pointerLiteralSymbol];
    },

    clonePointer(obj_: SignalDeviceLike): UniffiHandle {
        const pointer = this.pointer(obj_);
        return uniffiCaller.rustCall(
            /*caller:*/ (callStatus) => nativeModule().ubrn_uniffi_signal_native_fn_clone_signaldevice(pointer, callStatus),
            /*liftString:*/ FfiConverterString.lift
        );
    },

    freePointer(pointer: UniffiHandle): void {
        uniffiCaller.rustCall(
            /*caller:*/ (callStatus) => nativeModule().ubrn_uniffi_signal_native_fn_free_signaldevice(pointer, callStatus),
            /*liftString:*/ FfiConverterString.lift
        );
    },

    isConcreteType(obj_: any): obj_ is SignalDeviceLike {
        return obj_[destructorGuardSymbol] && obj_[uniffiTypeNameSymbol] === "SignalDevice";
    },
}})();
const FfiConverterTypeSignalDevice = new FfiConverterObject(uniffiTypeSignalDeviceObjectFactory);


/**
 * This should be called before anything else.
 *
 * It is likely that this is being done for you by the library's `index.ts`.
 *
 * It checks versions of uniffi between when the Rust scaffolding was generated
 * and when the bindings were generated.
 *
 * It also initializes the machinery to enable Rust to talk back to Javascript.
 */
function uniffiEnsureInitialized() {
    // Get the bindings contract version from our ComponentInterface
    const bindingsContractVersion = 30;
    // Get the scaffolding contract version by calling the into the dylib
    const scaffoldingContractVersion = nativeModule().ubrn_ffi_signal_native_uniffi_contract_version();
    if (bindingsContractVersion !== scaffoldingContractVersion) {
        throw new UniffiInternalError.ContractVersionMismatch(scaffoldingContractVersion, bindingsContractVersion);
    }
    if (nativeModule().ubrn_uniffi_signal_native_checksum_constructor_signaldevice_new() !== 56313) {
        throw new UniffiInternalError.ApiChecksumMismatch("uniffi_signal_native_checksum_constructor_signaldevice_new");
    }
    if (nativeModule().ubrn_uniffi_signal_native_checksum_method_signaldevice_decrypt() !== 1868) {
        throw new UniffiInternalError.ApiChecksumMismatch("uniffi_signal_native_checksum_method_signaldevice_decrypt");
    }
    if (nativeModule().ubrn_uniffi_signal_native_checksum_method_signaldevice_encrypt() !== 27497) {
        throw new UniffiInternalError.ApiChecksumMismatch("uniffi_signal_native_checksum_method_signaldevice_encrypt");
    }
    if (nativeModule().ubrn_uniffi_signal_native_checksum_method_signaldevice_establish_session() !== 51602) {
        throw new UniffiInternalError.ApiChecksumMismatch("uniffi_signal_native_checksum_method_signaldevice_establish_session");
    }
    if (nativeModule().ubrn_uniffi_signal_native_checksum_method_signaldevice_generate_prekey_bundle() !== 8421) {
        throw new UniffiInternalError.ApiChecksumMismatch("uniffi_signal_native_checksum_method_signaldevice_generate_prekey_bundle");
    }
    if (nativeModule().ubrn_uniffi_signal_native_checksum_method_signaldevice_identity_public_key_base64() !== 52526) {
        throw new UniffiInternalError.ApiChecksumMismatch("uniffi_signal_native_checksum_method_signaldevice_identity_public_key_base64");
    }

    }

export default Object.freeze({
  initialize: uniffiEnsureInitialized,
  converters: {
    FfiConverterTypeEncryptedEnvelope,
    FfiConverterTypePreKeyBundleData,
    FfiConverterTypeSignalDevice,
    FfiConverterTypeSignalNativeError,
  }
});